# Changelog

Все значимые изменения в проекте документируются в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/),
проект следует [Semantic Versioning](https://semver.org/lang/ru/).

## [1.0.5] — 2026-05-08

### Исправлено
- Tooltip больше не показывается при клике на радио-кнопки (например,
  при выборе звезды в рейтинге через `<label><input type="radio">...</label>`).
  Раньше клик по `<label>` фокусировал radio-input, тот становился
  `document.activeElement`, и проверка `form.contains(ae)` считала это
  «взаимодействием с формой». Теперь фокусом-триггером считается только
  элемент, подходящий под `fillableSelector`.

## [1.0.4] — 2026-05-08

### Добавлено
- Опция `submitSelector` — настройка селектора submit-элементов.
  Полезно, если на сайте кнопки сделаны как `<input type="button">`,
  ссылки или div'ы. Внешние кнопки через `form="<id>"` поддерживаются
  автоматически: к каждой части переданного селектора добавляется
  фильтр `[form="<id>"]` (с корректной обработкой запятых внутри
  `:is(...)`/`[attr="a,b"]`).
- Опция `fillableSelector` — какие поля считать «заполняемыми»
  для tooltip-режима. По умолчанию: текстовые/числовые/дата-time типы +
  `textarea` и `select`. Радио и чекбоксы исключены.
- Опция `minMeaningfulChars` — минимум значимых символов в значении
  поля, чтобы считать его действительно заполненным. По умолчанию
  `{ tel: 2, _default: 1 }`.

### Изменено
- `isFormFilled()` теперь не считает «заполненными» радио и чекбоксы:
  выбор звёздного рейтинга или подписки на рассылку больше не показывает
  tooltip согласия.
- При проверке текстовых полей плейсхолдеры маски (`_`, `#`, `*`)
  и форматирующие символы (`+ - ( ) . , / \\` и пробелы) отбрасываются
  перед измерением длины. Это решает проблему «висящего» tooltip на формах
  с phone-маской типа `+7 (___) ___-__-__`.
- Фокус на самой submit-кнопке больше не считается признаком
  «пользователь начал заполнять форму».

## [1.0.3] — 2026-05-07

### Изменено
- В `auto`-режиме при конвертации декларативного блока в активный
  содержимое блока теперь полностью заменяется на отрендеренный шаблон
  `options.text` (раньше оставался старый текст «Нажимая кнопку…», что
  юридически противоречило наличию чекбокса).
- URL ссылки в блоке сохраняется из исходной разметки, если она там была;
  иначе используется `options.policyUrl`.
- Tooltip теперь использует `position: fixed` и `z-index: 2147483647`,
  плюс при показе автоматически поднимается выше любого видимого элемента
  под якорем (через `elementsFromPoint`). Это решает проблему перекрытия
  tooltip модалками с высоким z-index.

## [1.0.2] — 2026-05-07

### Исправлено
- В `auto`-режиме при найденном декларативном блоке («Нажимая кнопку,
  вы соглашаетесь…») теперь действительно вставляется активный
  `<input type="checkbox">` и к нему привязывается блокировка submit-кнопок.
  Раньше библиотека только правила/добавляла ссылку, оставляя блок пассивным.

### Добавлено
- Опция `autoInjectCheckbox` (по умолчанию `true`) — управляет вставкой
  чекбокса в найденный декларативный блок.
- Класс `cc-converted-block` на блоке после конвертации и
  `cc-injected-input` на инжектированном `<input>` — для возможной
  кастомной стилизации.

## [1.0.0] — 2026-05-07

### Добавлено
- Первый публичный релиз.
- Четыре режима вставки: `auto`, `append`, `after-submit`, `tooltip`.
- Auto-режим с поиском существующего блока согласия по ключевым словам.
- Подмена `href` у политики конфиденциальности в найденном блоке.
- Автоматическая блокировка submit-кнопок (включая `form="id"`-привязки).
- Перехват `submit` в capture-фазе (защита от программного `form.submit()`).
- `MutationObserver` для динамически добавленных форм.
- Tooltip-режим с отслеживанием видимости через `IntersectionObserver`.
- UMD-обёртка (AMD / CommonJS / global).
